# URL público do frontend (usado no CORS e redirecionamentos)
FRONTEND_URL=http://localhost:3000

# Porta do backend
PORT=7788

# Idioma padrão
DEFAULT_LANGUAGE=pt-BR

# Segredo de sessão (trocar por um valor forte em produção)
SESSION_SECRET=change_me_to_a_strong_secret

# Rounds do bcrypt (número inteiro)
BCRYPT_ROUNDS=10
